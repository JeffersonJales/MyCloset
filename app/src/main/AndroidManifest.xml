<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mycloset">
    <uses-permission android:name="android.permission.CAMERA" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_my_closet"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_my_closet_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyCloset">

        <activity
            android:name=".CreateUserActivity"
            android:exported="true" />
        <activity
            android:name=".ListaitemsClosetActivity"
            android:exported="true" />
        <activity
            android:name=".CadastrarItemActivity"
            android:exported="true" />
        <activity
            android:name=".VisualizaritemActivity"
            android:exported="true"/>

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>